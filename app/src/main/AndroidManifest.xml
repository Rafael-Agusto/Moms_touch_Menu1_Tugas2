<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Moms_touch_Menu1"
        tools:targetApi="31">

        <activity
            android:name=".MenuRecyclerView"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Declare FoodOrderActivity -->
        <activity android:name="com.example.moms_touch_menu1.FoodOrderActivity" />

        <!-- Declare PesananActivity -->
        <activity android:name="com.example.moms_touch_menu1.PesananActivity" />

    </application>

</manifest>
